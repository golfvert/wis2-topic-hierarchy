on:
  workflow_dispatch:

jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - name: 'Setup jq'
        uses: dcarbone/install-jq-action@v2
        with:
          version: 1.7.1
          force: true

      - name: 'Check jq'
        # language=sh
        run: |
          which jq
          jq --version
